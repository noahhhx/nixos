# NixOS Configuration

NixOS install from a git repo using flakes and home-manager.

## Structure

```
nixos/
├── flake.nix                    # Flake entry point
└── .gitignore
```

## Automated Install